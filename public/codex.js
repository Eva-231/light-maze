import {GRADES,gearGrade,ENCHANTS,enchantText} from './equipment-crafting.js';
import {RELICS,RARITIES,relicDescription} from './relics.js';
import {POWERS,SYNERGIES,composePowers,awakened} from './abyss-gear.js';
import {ENEMY_TYPES} from './enemies.js';
import {MUTATIONS} from './abyss-rules.js';
import {TRAP_TYPES} from './expedition.js';
export const CODEX_CATEGORIES={equipment:'装備',grades:'深淵グレード',enchants:'エンチャント',mythic:'MYTHIC',effects:'特殊効果',synergies:'シナジー',enemies:'敵',mutations:'敵変異',traps:'罠',excavations:'発掘物'};
export function discover(progress,category,id){progress.codex??={};progress.codex[category]??=[];if(progress.codex[category].includes(id))return false;progress.codex[category].push(id);return true;}
export function discoverLoadout(progress){const fresh=[];for(const item of progress.inventory||[]){discover(progress,'equipment',item.type);const d=RELICS[item.type];if(gearGrade(item))discover(progress,'grades',gearGrade(item));if(d.mythic)discover(progress,'mythic',item.type);for(const e of item.enchants||[]){discover(progress,'enchants',e.key==='power'?e.power:e.key);if(e.key==='power')discover(progress,'effects',e.power);}for(const k of [...(d.powers||[]),...(item.extraPowers||[])])discover(progress,'effects',k);if(item.evolved)discover(progress,'evolutions',d.key);}
 const items=(progress.equipped||[]).map(id=>progress.inventory.find(i=>i.id===id)).filter(Boolean),powers=composePowers(items,RELICS).powers;
 for(const s of awakened(powers))if(discover(progress,'synergies',s.key))fresh.push(s);return fresh;
}
export function synergyStatus(progress){const items=(progress.equipped||[]).map(id=>progress.inventory.find(i=>i.id===id)).filter(Boolean),powers=composePowers(items,RELICS).powers;return SYNERGIES.map(s=>({...s,count:s.requires.filter(k=>powers.includes(k)).length,known:!s.hidden||progress.codex?.synergies?.includes(s.key),parts:s.requires.map(k=>({key:k,name:POWERS.find(p=>p.key===k)?.name||k,equipped:powers.includes(k)}))}));}
export function codexEntries(progress,category){const seen=progress.codex?.[category]||[];
 if(category==='synergies')return synergyStatus(progress).map(s=>({id:s.key,name:s.known?s.name:'未知の共鳴',found:s.known||s.count>0,discovered:progress.codex?.synergies?.includes(s.key)||false,effect:s.count+'/3 · '+(s.known?s.parts.map(p=>p.name+(p.equipped?' ✓':' ×')).join(' / '):'初めて覚醒すると条件を公開'),detail:s.known?s.effect:'???'}));
 let rows=[];if(category==='mythic')return codexEntries(progress,'equipment').filter(e=>RELICS[e.id].mythic);
 if(category==='grades')rows=GRADES.map((name,i)=>({id:i+1,name:'Abyss Grade '+name,effect:'付加性能 ×'+(1+i*.1).toFixed(1)+'・特殊攻撃補正 +'+i*4+'%・最大ロール '+(60+i*10),min:'主な階層 '+[10,30,50,70,90][i]+'F〜',detail:'合成素材倍率 ×'+(1.18+(i+1)*.035).toFixed(3)}));
 if(category==='enchants')rows=[...ENCHANTS.map(e=>({id:e.key,name:e.name,effect:'素材のレアリティと深淵Gradeで強化。最大4枠。同種特効は乗算、上限×3。覚醒条件には使いません。'})),...POWERS.map(p=>({id:p.key,name:p.name,effect:p.effect+' / 特殊能力エンチャント：覚醒条件に使用可能'}))];
 if(category==='equipment')rows=RELICS.map((r,i)=>({id:i,name:r.key==='eater'&&progress.codex?.evolutions?.includes('eater')?'深淵王喰らい':r.name,effect:r.effect+(r.key==='eater'&&progress.codex?.evolutions?.includes('eater')?' / 進化済み：最終火力×1.4':''),min:r.minFloor?('深淵 '+r.minFloor+'F〜'):r.exclusive?'本編7面以降':'本編・通常鑑定',detail:r.mythic?'MYTHIC · 付加性能最大100・強化+5':r.abyss?'深度補正 最大20・付加性能100・強化+5':'LEGEND · 付加性能最大100・強化+5',tier:r.mythic?5:r.abyss?4:3}));
 if(category==='effects')rows=POWERS.map(p=>({...p,id:p.key,min:'深淵 '+p.minFloor+'F〜'}));if(category==='enemies')rows=ENEMY_TYPES.map((e,i)=>({id:i,name:e.name,effect:e.hint,min:'本編 / 深淵',detail:'Lv1〜10 · Lv3/5/7/10で強化'}));
 if(category==='mutations')rows=MUTATIONS.map(m=>({...m,min:'敵Lv'+m.min+'以上'}));if(category==='traps')rows=TRAP_TYPES.map(t=>({...t,id:t.key,effect:t.hint,min:'本編5面以降 / 深淵'}));
 if(category==='excavations')rows=[{id:0,name:'微かな鉱脈',effect:'持ち帰って鑑定する遺物。通常の伝説率1%'},{id:1,name:'紫の鉱脈',effect:'希少・特級を探す。伝説率1%'},{id:2,name:'深層の金脈',effect:'深淵10/30/50/70/90Fで品質が段階的に上昇'}];
 return rows.map(r=>({...r,found:seen.includes(r.id)}));
}
// Lightweight portraits share the equipment silhouettes, rarity colors and Mythic motifs.
function equipmentPortrait(entry){const d=RELICS[entry.id],color=entry.found?(d.curse?'#aa76cf':RARITIES[entry.tier].color):'#3e5657',shape=d.slot===0?'<path d="M45 68L72 25M41 68L48 72"/><path d="M72 10L83 24L71 35L61 23Z"/>':d.slot===1?'<path d="M45 23L60 12L76 23L72 54L60 66L49 54Z"/><path d="M60 22V52M51 32H70"/>':'<path d="M49 28H72L68 61H53ZM52 25Q60 5 69 25M54 64H68"/><path d="M60 35L66 44L61 55L57 44Z"/>';const rings=entry.found&&entry.tier>=4?'<circle cx="61" cy="40" r="31" opacity=".4"/><circle cx="61" cy="40" r="35" stroke-dasharray="2 7" opacity=".6"/>':'';const motif=entry.found&&d.mythic?Array.from({length:d.key==='reincarnation'?4:d.key==='amaterasu'?8:d.key==='eater'?3:6},(_,i)=>'<path d="M61 1L63 6L61 11L59 6Z" transform="rotate('+i*(d.key==='amaterasu'?45:d.key==='kaleidoscope'?60:90)+' 61 40)"/>').join(''):'';return '<div class="equipment-portrait" style="--portrait:'+color+'"><svg viewBox="0 0 122 80" aria-hidden="true"><g fill="none" stroke="'+color+'" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">'+rings+shape+motif+'</g></svg></div>';}
export function createCodex({getProgress,openDialog}){const $=id=>document.getElementById(id);let tab='equipment';function render(){const p=getProgress();discoverLoadout(p);$('codex-summary').textContent=Object.entries(CODEX_CATEGORIES).map(([k,name])=>{const entries=codexEntries(p,k);return name+' '+entries.filter(e=>e.discovered??e.found).length+'/'+entries.length;}).join(' · ');
 $('codex-tabs').innerHTML=Object.entries(CODEX_CATEGORIES).map(([id,name])=>`<button data-codex-tab="${id}" aria-pressed="${tab===id}">${name}</button>`).join('');$('codex-list').innerHTML=codexEntries(p,tab).map(e=>`<article class="codex-entry ${e.found?'':'unknown'}">${tab==='equipment'||tab==='mythic'?equipmentPortrait(e):''}<h3><span class="codex-glyph" style="--gear-color:${RARITIES[e.tier||0]?.color}">${e.found?'✧':'◆'}</span>${e.found?e.name:'???'}</h3><p>${e.found?e.effect||'':''}</p><p>${e.found?[e.min,e.detail].filter(Boolean).join(' / '):'未発見'}</p></article>`).join('');}
 $('codex-open').addEventListener('click',()=>{render();openDialog('codex-dialog');});$('codex-tabs').addEventListener('click',e=>{const b=e.target.closest('[data-codex-tab]');if(b){tab=b.dataset.codexTab;render();}});return{render};}
